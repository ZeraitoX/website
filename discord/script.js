document.addEventListener("DOMContentLoaded", () => {
    const joinButton = document.querySelector(".cta a");
    joinButton.addEventListener("click", () => {
        alert("You are being redirected to join the Discord server!");
    });
});

